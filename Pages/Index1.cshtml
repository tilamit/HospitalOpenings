@page
@model HospitalOpenings.Pages.Index1Model
@{
}

<h1>This is it.</h1>

@foreach (var item in @Model.GetSampleData())
{
    @item.Id; @:&nbsp;
    @item.Name; @:&nbsp;
    @item.Address; @:&nbsp;
    @item.Email;
    <br />
}


